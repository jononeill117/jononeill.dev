---
import DemoFrame from "./DemoFrame.astro";
---

<DemoFrame
  class="stoa-demo"
  aspect="16 / 10"
  system="Data → underwriting → decision"
  cta="Open a Portico file. This is the queue 40 analysts worked."
  caption="Interactive walkthrough"
>
  <div class="sp" data-sp>
    <aside class="sp__rail" aria-hidden="true">
      <span class="sp__rail-mark">P</span>
      <span class="sp__rail-dot is-active"></span>
      <span class="sp__rail-dot"></span>
      <span class="sp__rail-dot"></span>
      <span class="sp__rail-dot"></span>
      <span class="sp__rail-dot"></span>
      <span class="sp__rail-dot"></span>
    </aside>

    <div class="sp__app">
      <header class="sp__top">
        <div class="sp__brand">
          <span class="sp__logo" aria-hidden="true"></span>
          <div class="sp__brand-text">
            <span class="sp__product">Portico</span>
            <span class="sp__module">Evaluations</span>
          </div>
        </div>
        <nav class="sp__tabs" aria-label="Workflow">
          <span class="sp__tab">Pre-screening</span>
          <span class="sp__tab is-active">Home Fact Verification</span>
          <span class="sp__tab">Rental Comps</span>
          <span class="sp__tab">ARV Comps</span>
          <span class="sp__tab">Wrap Up</span>
        </nav>
        <div class="sp__user" aria-hidden="true">
          <span class="sp__user-icon"></span>
          <span class="sp__avatar">CM</span>
        </div>
      </header>

      <p class="sp__status" data-sp-step>Waiting for file</p>

      <div class="sp__workspace">
        <section class="sp__details" aria-label="Home fact verification">
          <div class="sp__meta">
            <button type="button" class="sp__back" tabindex="-1" aria-hidden="true">&lt; Back</button>
            <div class="sp__scores" data-sp-scores>
              <span class="sp__score sp__score--deal" data-sp-score="deal">Deal Score: -</span>
              <span class="sp__score sp__score--partner" data-sp-score="partner">Partner Score: -</span>
              <span class="sp__score sp__score--entity" data-sp-score="entity">Entity Score: -</span>
            </div>
          </div>

          <h3 class="sp__addr" data-sp-addr>Opening file…</h3>
          <p class="sp__facts" data-sp-facts></p>

          <div class="sp__summary">
            <div class="sp__desc" data-sp-desc>
              <p class="sp__desc-title">Property Description</p>
              <p class="sp__desc-body" data-sp-desc-body>
                Quiet single-family lot with solid bones for a light reno. Kitchen and baths need
                updates; roof and HVAC recent. Close to schools and retail.
                <span class="sp__more">…more</span>
              </p>
            </div>

            <div class="sp__val" data-sp-val-card>
              <p class="sp__val-label">Stoa Valuation</p>
              <p class="sp__val-price">
                <strong data-sp-val>$-</strong>
                <span class="sp__val-psf" data-sp-psf></span>
              </p>
              <div class="sp__range" data-sp-range aria-hidden="true">
                <div class="sp__range-track">
                  <span class="sp__range-fill"></span>
                  <span class="sp__range-marker" data-sp-marker></span>
                </div>
                <div class="sp__range-labels">
                  <span data-sp-low>Low Estimate</span>
                  <span data-sp-high>High Estimate</span>
                </div>
              </div>
            </div>
          </div>

          <div class="sp__table-wrap" data-sp-table>
            <div class="sp__table-head">
              <h4 class="sp__table-title">Property Details</h4>
              <span class="sp__feed-pill" data-sp-feed-pill>Feeds idle</span>
            </div>
            <div class="sp__table-scroll">
              <table class="sp__table">
                <thead>
                  <tr>
                    <th>Details</th>
                    <th>Post Reno</th>
                    <th data-sp-col="public">Public Record</th>
                    <th data-sp-col="mls">MLS</th>
                    <th data-sp-col="third">3rd Party</th>
                    <th data-sp-col="final">Final Data</th>
                    <th>Contingency</th>
                  </tr>
                </thead>
                <tbody>
                  <tr data-sp-row="beds" class="sp__row--flag">
                    <td>Bedrooms</td>
                    <td data-sp-cell="beds-reno">-</td>
                    <td data-sp-cell="beds-public">-</td>
                    <td data-sp-cell="beds-mls">-</td>
                    <td data-sp-cell="beds-third">-</td>
                    <td>
                      <button type="button" class="sp__final" data-sp-nudge="beds" disabled>-</button>
                    </td>
                    <td>
                      <input type="checkbox" class="sp__check" data-sp-check="beds" disabled tabindex="-1" />
                    </td>
                  </tr>
                  <tr data-sp-row="baths">
                    <td>Bathrooms</td>
                    <td data-sp-cell="baths-reno">-</td>
                    <td data-sp-cell="baths-public">-</td>
                    <td data-sp-cell="baths-mls">-</td>
                    <td data-sp-cell="baths-third">-</td>
                    <td>
                      <button type="button" class="sp__final" data-sp-nudge="baths" disabled>-</button>
                    </td>
                    <td>
                      <input type="checkbox" class="sp__check" data-sp-check="baths" disabled tabindex="-1" />
                    </td>
                  </tr>
                  <tr data-sp-row="stories">
                    <td>Story Count</td>
                    <td data-sp-cell="stories-reno">-</td>
                    <td data-sp-cell="stories-public">-</td>
                    <td data-sp-cell="stories-mls">-</td>
                    <td data-sp-cell="stories-third">-</td>
                    <td>
                      <button type="button" class="sp__final" data-sp-nudge="stories" disabled>-</button>
                    </td>
                    <td>
                      <input type="checkbox" class="sp__check" data-sp-check="stories" disabled tabindex="-1" />
                    </td>
                  </tr>
                  <tr data-sp-row="gla">
                    <td>GLA</td>
                    <td data-sp-cell="gla-reno">-</td>
                    <td data-sp-cell="gla-public">-</td>
                    <td data-sp-cell="gla-mls">-</td>
                    <td data-sp-cell="gla-third">-</td>
                    <td>
                      <button type="button" class="sp__final" data-sp-nudge="gla" disabled>-</button>
                    </td>
                    <td>
                      <input type="checkbox" class="sp__check" data-sp-check="gla" disabled tabindex="-1" />
                    </td>
                  </tr>
                  <tr data-sp-row="basement">
                    <td>Basement</td>
                    <td data-sp-cell="basement-reno">-</td>
                    <td data-sp-cell="basement-public">-</td>
                    <td data-sp-cell="basement-mls">-</td>
                    <td data-sp-cell="basement-third">-</td>
                    <td>
                      <button type="button" class="sp__final" data-sp-nudge="basement" disabled>-</button>
                    </td>
                    <td>
                      <input type="checkbox" class="sp__check" data-sp-check="basement" disabled tabindex="-1" />
                    </td>
                  </tr>
                  <tr data-sp-row="lot" class="sp__row--flag">
                    <td>Lot Size</td>
                    <td data-sp-cell="lot-reno">-</td>
                    <td data-sp-cell="lot-public">-</td>
                    <td data-sp-cell="lot-mls">-</td>
                    <td data-sp-cell="lot-third">-</td>
                    <td>
                      <button type="button" class="sp__final" data-sp-nudge="lot" disabled>-</button>
                    </td>
                    <td>
                      <input type="checkbox" class="sp__check" data-sp-check="lot" disabled tabindex="-1" />
                    </td>
                  </tr>
                  <tr data-sp-row="year">
                    <td>Year Built</td>
                    <td data-sp-cell="year-reno">-</td>
                    <td data-sp-cell="year-public">-</td>
                    <td data-sp-cell="year-mls">-</td>
                    <td data-sp-cell="year-third">-</td>
                    <td>
                      <button type="button" class="sp__final" data-sp-nudge="year" disabled>-</button>
                    </td>
                    <td>
                      <input type="checkbox" class="sp__check" data-sp-check="year" disabled tabindex="-1" />
                    </td>
                  </tr>
                  <tr data-sp-row="pool">
                    <td>Pool</td>
                    <td data-sp-cell="pool-reno">-</td>
                    <td data-sp-cell="pool-public">-</td>
                    <td data-sp-cell="pool-mls">-</td>
                    <td data-sp-cell="pool-third">-</td>
                    <td>
                      <button type="button" class="sp__final" data-sp-nudge="pool" disabled>-</button>
                    </td>
                    <td>
                      <input type="checkbox" class="sp__check" data-sp-check="pool" disabled tabindex="-1" />
                    </td>
                  </tr>
                  <tr data-sp-row="septic">
                    <td>Septic System</td>
                    <td data-sp-cell="septic-reno">-</td>
                    <td data-sp-cell="septic-public">-</td>
                    <td data-sp-cell="septic-mls">-</td>
                    <td data-sp-cell="septic-third">-</td>
                    <td>
                      <button type="button" class="sp__final" data-sp-nudge="septic" disabled>-</button>
                    </td>
                    <td>
                      <input type="checkbox" class="sp__check" data-sp-check="septic" disabled tabindex="-1" />
                    </td>
                  </tr>
                </tbody>
              </table>
            </div>
          </div>

          <ul class="sp__flags" data-sp-flags hidden>
            <li class="sp__flag" data-sp-flag="beds">Bedrooms mismatch across sources</li>
            <li class="sp__flag" data-sp-flag="lot">Lot size variance flagged</li>
            <li class="sp__flag" data-sp-flag="avm">Valuation band within 6%</li>
          </ul>

          <div class="sp__actions" data-sp-actions>
            <button type="button" class="sp__accept" data-sp-accept disabled>Accept</button>
            <button type="button" class="sp__recalc" data-sp-recalc hidden disabled>Recalc</button>
            <button type="button" class="sp__replay" data-sp-replay hidden>Replay</button>
            <p class="sp__hint" data-sp-hint hidden>Nudge a Final Data cell, then Recalc.</p>
          </div>
        </section>

        <section class="sp__map" data-sp-map aria-label="Neighborhood heatmap">
          <div class="sp__map-chrome">
            <div class="sp__map-modes">
              <span class="sp__map-mode is-active">Map</span>
              <span class="sp__map-mode">Satellite</span>
              <span class="sp__map-mode">Street</span>
            </div>
            <div class="sp__map-layer-pill" data-sp-layer-pill>Crime</div>
            <div class="sp__map-zoom" aria-hidden="true">
              <span>+</span>
              <span>-</span>
            </div>
          </div>
          <div class="sp__map-canvas">
            <div class="sp__gmap" data-sp-gmap aria-hidden="true">
              <div class="sp__gmap-pan">
                <div class="sp__gmap-roads"></div>
                <div class="sp__gmap-park"></div>
                <div class="sp__gmap-water"></div>
                <span class="sp__gmap-label" style="left:12%;top:22%">Oak Ave</span>
                <span class="sp__gmap-label" style="left:48%;top:58%">Harbor Ln</span>
                <span class="sp__gmap-label sp__gmap-label--place" style="left:62%;top:30%">Riverside</span>
                <span class="sp__pin" data-sp-pin></span>
              </div>
              <svg class="sp__octs" viewBox="0 0 320 220" preserveAspectRatio="xMidYMid slice" data-sp-octs>
                <g data-sp-oct-layer="crime" class="is-active">
                  <polygon points="48,70 62,62 76,62 90,70 90,86 76,94 62,94 48,86" fill="#ef4444" opacity="0.55"></polygon>
                  <polygon points="90,86 104,78 118,78 132,86 132,102 118,110 104,110 90,102" fill="#f97316" opacity="0.5"></polygon>
                  <polygon points="132,70 146,62 160,62 174,70 174,86 160,94 146,94 132,86" fill="#fbbf24" opacity="0.45"></polygon>
                  <polygon points="174,86 188,78 202,78 216,86 216,102 202,110 188,110 174,102" fill="#ef4444" opacity="0.62"></polygon>
                  <polygon points="90,118 104,110 118,110 132,118 132,134 118,142 104,142 90,134" fill="#dc2626" opacity="0.58"></polygon>
                  <polygon points="132,134 146,126 160,126 174,134 174,150 160,158 146,158 132,150" fill="#fb923c" opacity="0.5"></polygon>
                  <polygon points="174,118 188,110 202,110 216,118 216,134 202,142 188,142 174,134" fill="#f59e0b" opacity="0.48"></polygon>
                  <polygon points="216,102 230,94 244,94 258,102 258,118 244,126 230,126 216,118" fill="#ef4444" opacity="0.55"></polygon>
                </g>
                <g data-sp-oct-layer="dom" hidden>
                  <polygon points="48,70 62,62 76,62 90,70 90,86 76,94 62,94 48,86" fill="#22c55e" opacity="0.5"></polygon>
                  <polygon points="90,86 104,78 118,78 132,86 132,102 118,110 104,110 90,102" fill="#4ade80" opacity="0.55"></polygon>
                  <polygon points="132,70 146,62 160,62 174,70 174,86 160,94 146,94 132,86" fill="#86efac" opacity="0.45"></polygon>
                  <polygon points="174,86 188,78 202,78 216,86 216,102 202,110 188,110 174,102" fill="#16a34a" opacity="0.6"></polygon>
                  <polygon points="90,118 104,110 118,110 132,118 132,134 118,142 104,142 90,134" fill="#bbf7d0" opacity="0.5"></polygon>
                  <polygon points="132,134 146,126 160,126 174,134 174,150 160,158 146,158 132,150" fill="#22c55e" opacity="0.55"></polygon>
                  <polygon points="174,118 188,110 202,110 216,118 216,134 202,142 188,142 174,134" fill="#15803d" opacity="0.58"></polygon>
                  <polygon points="216,102 230,94 244,94 258,102 258,118 244,126 230,126 216,118" fill="#4ade80" opacity="0.48"></polygon>
                </g>
                <g data-sp-oct-layer="fire" hidden>
                  <polygon points="48,70 62,62 76,62 90,70 90,86 76,94 62,94 48,86" fill="#fb923c" opacity="0.45"></polygon>
                  <polygon points="90,86 104,78 118,78 132,86 132,102 118,110 104,110 90,102" fill="#f97316" opacity="0.55"></polygon>
                  <polygon points="132,70 146,62 160,62 174,70 174,86 160,94 146,94 132,86" fill="#ea580c" opacity="0.62"></polygon>
                  <polygon points="174,86 188,78 202,78 216,86 216,102 202,110 188,110 174,102" fill="#c2410c" opacity="0.7"></polygon>
                  <polygon points="90,118 104,110 118,110 132,118 132,134 118,142 104,142 90,134" fill="#fdba74" opacity="0.48"></polygon>
                  <polygon points="132,134 146,126 160,126 174,134 174,150 160,158 146,158 132,150" fill="#f97316" opacity="0.58"></polygon>
                  <polygon points="174,118 188,110 202,110 216,118 216,134 202,142 188,142 174,134" fill="#9a3412" opacity="0.65"></polygon>
                  <polygon points="216,102 230,94 244,94 258,102 258,118 244,126 230,126 216,118" fill="#ea580c" opacity="0.55"></polygon>
                </g>
                <g data-sp-oct-layer="noise" hidden>
                  <polygon points="48,70 62,62 76,62 90,70 90,86 76,94 62,94 48,86" fill="#38bdf8" opacity="0.45"></polygon>
                  <polygon points="90,86 104,78 118,78 132,86 132,102 118,110 104,110 90,102" fill="#0ea5e9" opacity="0.55"></polygon>
                  <polygon points="132,70 146,62 160,62 174,70 174,86 160,94 146,94 132,86" fill="#0284c7" opacity="0.6"></polygon>
                  <polygon points="174,86 188,78 202,78 216,86 216,102 202,110 188,110 174,102" fill="#7dd3fc" opacity="0.48"></polygon>
                  <polygon points="90,118 104,110 118,110 132,118 132,134 118,142 104,142 90,134" fill="#0369a1" opacity="0.62"></polygon>
                  <polygon points="132,134 146,126 160,126 174,134 174,150 160,158 146,158 132,150" fill="#38bdf8" opacity="0.5"></polygon>
                  <polygon points="174,118 188,110 202,110 216,118 216,134 202,142 188,142 174,134" fill="#0ea5e9" opacity="0.55"></polygon>
                  <polygon points="216,102 230,94 244,94 258,102 258,118 244,126 230,126 216,118" fill="#bae6fd" opacity="0.45"></polygon>
                </g>
                <g data-sp-oct-layer="flood" hidden>
                  <polygon points="48,70 62,62 76,62 90,70 90,86 76,94 62,94 48,86" fill="#818cf8" opacity="0.5"></polygon>
                  <polygon points="90,86 104,78 118,78 132,86 132,102 118,110 104,110 90,102" fill="#6366f1" opacity="0.58"></polygon>
                  <polygon points="132,70 146,62 160,62 174,70 174,86 160,94 146,94 132,86" fill="#4f46e5" opacity="0.62"></polygon>
                  <polygon points="174,86 188,78 202,78 216,86 216,102 202,110 188,110 174,102" fill="#a5b4fc" opacity="0.48"></polygon>
                  <polygon points="90,118 104,110 118,110 132,118 132,134 118,142 104,142 90,134" fill="#4338ca" opacity="0.65"></polygon>
                  <polygon points="132,134 146,126 160,126 174,134 174,150 160,158 146,158 132,150" fill="#818cf8" opacity="0.52"></polygon>
                  <polygon points="174,118 188,110 202,110 216,118 216,134 202,142 188,142 174,134" fill="#6366f1" opacity="0.55"></polygon>
                  <polygon points="216,102 230,94 244,94 258,102 258,118 244,126 230,126 216,118" fill="#c7d2fe" opacity="0.45"></polygon>
                </g>
              </svg>
            </div>
          </div>
          <div class="sp__map-foot">
            <div class="sp__layers" data-sp-layers>
              <button type="button" class="sp__layer is-active" data-sp-layer="crime" tabindex="-1">Crime</button>
              <button type="button" class="sp__layer" data-sp-layer="dom" tabindex="-1">DOM</button>
              <button type="button" class="sp__layer" data-sp-layer="fire" tabindex="-1">Fire</button>
              <button type="button" class="sp__layer" data-sp-layer="noise" tabindex="-1">Noise</button>
              <button type="button" class="sp__layer" data-sp-layer="flood" tabindex="-1">Flood</button>
            </div>
            <span class="sp__view-images">Neighborhood risk</span>
          </div>
        </section>
      </div>
    </div>
  </div>
</DemoFrame>

<script>
  type SpFeed = "public" | "mls" | "third" | "final";

  type SpRowKey =
    | "beds"
    | "baths"
    | "stories"
    | "gla"
    | "basement"
    | "lot"
    | "year"
    | "pool"
    | "septic";

  type SpRowData = {
    reno: string;
    public: string;
    mls: string;
    third: string;
    final: string;
    contingency: boolean;
  };

  const ADDRESS = "14436 Williamsburg Manor Drive, Longcityname, ST, 84657";
  const FACTS = "Single Family Detached · 2 Beds · 1 Bath · 1,500 SqFt. · Built 1965 · Stoa ID #4418";

  const ROW_KEYS: SpRowKey[] = [
    "beds",
    "baths",
    "stories",
    "gla",
    "basement",
    "lot",
    "year",
    "pool",
    "septic",
  ];

  const TABLE: Record<SpRowKey, SpRowData> = {
    beds: { reno: "2", public: "2", mls: "1", third: "-", final: "2", contingency: true },
    baths: { reno: "3", public: "3", mls: "0", third: "-", final: "2", contingency: false },
    stories: { reno: "-", public: "-", mls: "2", third: "2", final: "2", contingency: false },
    gla: { reno: "2000", public: "2000", mls: "1814", third: "-", final: "1500", contingency: false },
    basement: { reno: "-", public: "-", mls: "No", third: "No", final: "No", contingency: false },
    lot: {
      reno: "5643 sqft.",
      public: "5000 sqft.",
      mls: "5643 sqft.",
      third: "5000 sqft.",
      final: "5000 sqft.",
      contingency: true,
    },
    year: { reno: "-", public: "1952", mls: "1953", third: "-", final: "1965", contingency: false },
    pool: { reno: "No", public: "Yes", mls: "No", third: "-", final: "No", contingency: false },
    septic: { reno: "-", public: "-", mls: "No", third: "-", final: "No", contingency: false },
  };

  const NUDGE_CYCLES: Partial<Record<SpRowKey, string[]>> = {
    beds: ["2", "3", "2"],
    gla: ["1500", "1620", "1480"],
    lot: ["5000 sqft.", "5643 sqft.", "5200 sqft."],
    year: ["1965", "1953", "1960"],
  };

  const VALUATIONS = [
    { price: "$392,547", psf: "$230/sqft.", low: "Low Estimate $379,465 ($222/sqft.)", high: "High Estimate $405,630 ($227/sqft.)" },
    { price: "$384,200", psf: "$225/sqft.", low: "Low Estimate $371,100 ($217/sqft.)", high: "High Estimate $397,400 ($233/sqft.)" },
    { price: "$401,100", psf: "$235/sqft.", low: "Low Estimate $388,050 ($227/sqft.)", high: "High Estimate $414,200 ($243/sqft.)" },
  ] as const;

  const FEEDS: { id: SpFeed; label: string; progressMs: number; resolveMs: number }[] = [
    { id: "public", label: "Public Record", progressMs: 1320, resolveMs: 920 },
    { id: "mls", label: "MLS", progressMs: 1380, resolveMs: 940 },
    { id: "third", label: "3rd Party", progressMs: 1300, resolveMs: 900 },
    { id: "final", label: "Final Data", progressMs: 1420, resolveMs: 960 },
  ];

  function sleep(ms: number) {
    return new Promise((r) => setTimeout(r, ms));
  }

  function wireStoa(root: HTMLElement) {
    const frame = root.closest<HTMLElement>("[data-demo-frame]");
    if (!frame || frame.hasAttribute("data-sp-wired")) return;
    frame.setAttribute("data-sp-wired", "true");

    const stepEl = root.querySelector<HTMLElement>("[data-sp-step]");
    const addrEl = root.querySelector<HTMLElement>("[data-sp-addr]");
    const factsEl = root.querySelector<HTMLElement>("[data-sp-facts]");
    const descBody = root.querySelector<HTMLElement>("[data-sp-desc-body]");
    const desc = root.querySelector<HTMLElement>("[data-sp-desc]");
    const valCard = root.querySelector<HTMLElement>("[data-sp-val-card]");
    const valEl = root.querySelector<HTMLElement>("[data-sp-val]");
    const psfEl = root.querySelector<HTMLElement>("[data-sp-psf]");
    const lowEl = root.querySelector<HTMLElement>("[data-sp-low]");
    const highEl = root.querySelector<HTMLElement>("[data-sp-high]");
    const rangeEl = root.querySelector<HTMLElement>("[data-sp-range]");
    const feedPill = root.querySelector<HTMLElement>("[data-sp-feed-pill]");
    const flags = root.querySelector<HTMLElement>("[data-sp-flags]");
    const mapEl = root.querySelector<HTMLElement>("[data-sp-map]");
    const layerPill = root.querySelector<HTMLElement>("[data-sp-layer-pill]");
    const layerBtns = Array.from(root.querySelectorAll<HTMLButtonElement>("[data-sp-layer]"));
    const octLayers = Array.from(root.querySelectorAll<SVGGElement>("[data-sp-oct-layer]"));
    const acceptBtn = root.querySelector<HTMLButtonElement>("[data-sp-accept]");
    const recalcBtn = root.querySelector<HTMLButtonElement>("[data-sp-recalc]");
    const replayBtn = root.querySelector<HTMLButtonElement>("[data-sp-replay]");
    const hint = root.querySelector<HTMLElement>("[data-sp-hint]");
    const nudgeBtns = Array.from(root.querySelectorAll<HTMLButtonElement>("[data-sp-nudge]"));

    type SpOverlay = "crime" | "dom" | "fire" | "noise" | "flood";
    const OVERLAYS: { id: SpOverlay; label: string }[] = [
      { id: "crime", label: "Crime" },
      { id: "dom", label: "DOM" },
      { id: "fire", label: "Fire risk" },
      { id: "noise", label: "Noise" },
      { id: "flood", label: "Flood" },
    ];

    let running = false;
    let token = 0;
    let tryMode = false;
    let dirty = false;
    let recalcing = false;
    let valuationIdx = 0;
    let overlayIdx = 0;
    let overlayTimer: ReturnType<typeof setInterval> | null = null;
    const nudgeIdx: Partial<Record<SpRowKey, number>> = {};

    const setOverlay = (id: SpOverlay) => {
      const meta = OVERLAYS.find((o) => o.id === id) ?? OVERLAYS[0]!;
      overlayIdx = OVERLAYS.findIndex((o) => o.id === id);
      if (layerPill) layerPill.textContent = meta.label;
      layerBtns.forEach((btn) => {
        btn.classList.toggle("is-active", btn.getAttribute("data-sp-layer") === id);
      });
      octLayers.forEach((g) => {
        const on = g.getAttribute("data-sp-oct-layer") === id;
        g.toggleAttribute("hidden", !on);
        g.classList.toggle("is-active", on);
      });
    };

    const stopOverlayCycle = () => {
      if (overlayTimer) {
        clearInterval(overlayTimer);
        overlayTimer = null;
      }
    };

    const startOverlayCycle = () => {
      stopOverlayCycle();
      setOverlay(OVERLAYS[overlayIdx]?.id ?? "crime");
      overlayTimer = setInterval(() => {
        overlayIdx = (overlayIdx + 1) % OVERLAYS.length;
        setOverlay(OVERLAYS[overlayIdx]!.id);
      }, 2200);
    };

    const setStep = (text: string) => {
      if (stepEl) stepEl.textContent = text;
    };

    const setValuation = (idx: number, working = false) => {
      const v = VALUATIONS[idx] ?? VALUATIONS[0]!;
      valuationIdx = idx;
      if (valEl) valEl.textContent = working ? "…" : v.price;
      if (psfEl) psfEl.textContent = working ? "" : v.psf;
      if (lowEl) lowEl.textContent = working ? "Low Estimate" : v.low;
      if (highEl) highEl.textContent = working ? "High Estimate" : v.high;
      valCard?.classList.toggle("is-working", working);
      valCard?.classList.toggle("is-live", !working && idx >= 0);
      rangeEl?.classList.toggle("is-live", !working);
    };

    const clearCells = () => {
      ROW_KEYS.forEach((key) => {
        (["reno", "public", "mls", "third"] as const).forEach((col) => {
          const el = root.querySelector(`[data-sp-cell='${key}-${col}']`);
          if (el) {
            el.textContent = "-";
            el.classList.remove("is-working", "is-live");
          }
        });
        const btn = root.querySelector<HTMLButtonElement>(`[data-sp-nudge='${key}']`);
        if (btn) {
          btn.textContent = "-";
          btn.disabled = true;
          btn.classList.remove("is-working", "is-live", "is-pulse", "is-nudge");
        }
        const check = root.querySelector<HTMLInputElement>(`[data-sp-check='${key}']`);
        if (check) {
          check.checked = false;
          check.disabled = true;
          check.classList.remove("is-live");
        }
        root.querySelector(`[data-sp-row='${key}']`)?.classList.remove("is-working", "is-live");
      });
      (["public", "mls", "third", "final"] as SpFeed[]).forEach((id) => {
        root.querySelector(`[data-sp-col='${id}']`)?.classList.remove("is-working", "is-live");
      });
    };

    const fillReno = () => {
      ROW_KEYS.forEach((key) => {
        const el = root.querySelector(`[data-sp-cell='${key}-reno']`);
        if (el) {
          el.textContent = TABLE[key].reno;
          el.classList.add("is-live");
        }
      });
    };

    const setFeedWorking = (id: SpFeed) => {
      root.querySelector(`[data-sp-col='${id}']`)?.classList.add("is-working");
      root.querySelector(`[data-sp-col='${id}']`)?.classList.remove("is-live");
      ROW_KEYS.forEach((key) => {
        if (id === "final") {
          const btn = root.querySelector(`[data-sp-nudge='${key}']`);
          btn?.classList.add("is-working");
          btn?.classList.remove("is-live");
          if (btn) btn.textContent = "…";
        } else {
          const el = root.querySelector(`[data-sp-cell='${key}-${id}']`);
          el?.classList.add("is-working");
          el?.classList.remove("is-live");
          if (el) el.textContent = "…";
        }
      });
    };

    const setFeedLive = (id: SpFeed) => {
      root.querySelector(`[data-sp-col='${id}']`)?.classList.remove("is-working");
      root.querySelector(`[data-sp-col='${id}']`)?.classList.add("is-live");
      ROW_KEYS.forEach((key) => {
        const row = TABLE[key];
        if (id === "final") {
          const btn = root.querySelector<HTMLButtonElement>(`[data-sp-nudge='${key}']`);
          if (btn) {
            btn.textContent = row.final;
            btn.classList.remove("is-working");
            btn.classList.add("is-live");
          }
        } else {
          const el = root.querySelector(`[data-sp-cell='${key}-${id}']`);
          if (el) {
            el.textContent = row[id];
            el.classList.remove("is-working");
            el.classList.add("is-live");
          }
        }
      });
    };

    const resetBoard = () => {
      setStep("Waiting for file");
      if (addrEl) addrEl.textContent = "Opening file…";
      if (factsEl) factsEl.textContent = "";
      desc?.classList.remove("is-live");
      if (descBody) descBody.style.opacity = "0.35";
      clearCells();
      setValuation(0, true);
      if (valEl) valEl.textContent = "$-";
      valCard?.classList.remove("is-working", "is-live");
      rangeEl?.classList.remove("is-live");
      if (feedPill) feedPill.textContent = "Feeds idle";
      if (flags) flags.hidden = true;
      flags?.querySelectorAll(".sp__flag").forEach((el) => {
        el.classList.remove("is-working", "is-live");
      });
      stopOverlayCycle();
      overlayIdx = 0;
      setOverlay("crime");
      mapEl?.classList.remove("is-live", "is-heat");
      root.querySelectorAll("[data-sp-score]").forEach((el) => {
        el.classList.remove("is-live");
        const kind = el.getAttribute("data-sp-score");
        if (kind === "deal") el.textContent = "Deal Score: -";
        if (kind === "partner") el.textContent = "Partner Score: -";
        if (kind === "entity") el.textContent = "Entity Score: -";
      });
      if (acceptBtn) {
        acceptBtn.disabled = true;
        acceptBtn.classList.remove("is-done");
        acceptBtn.textContent = "Accept";
        acceptBtn.hidden = false;
      }
      if (recalcBtn) {
        recalcBtn.hidden = true;
        recalcBtn.disabled = true;
      }
      if (replayBtn) replayBtn.hidden = true;
      if (hint) hint.hidden = true;
      tryMode = false;
      dirty = false;
      recalcing = false;
      valuationIdx = 0;
      Object.keys(nudgeIdx).forEach((k) => delete nudgeIdx[k as SpRowKey]);
    };

    const paintScores = () => {
      const deal = root.querySelector("[data-sp-score='deal']");
      const partner = root.querySelector("[data-sp-score='partner']");
      const entity = root.querySelector("[data-sp-score='entity']");
      if (deal) deal.textContent = "Deal Score: 92";
      if (partner) partner.textContent = "Partner Score: 77";
      if (entity) entity.textContent = "Entity Score: 90";
      root.querySelectorAll("[data-sp-score]").forEach((el) => el.classList.add("is-live"));
    };

    const paintContingencies = () => {
      ROW_KEYS.forEach((key) => {
        const check = root.querySelector<HTMLInputElement>(`[data-sp-check='${key}']`);
        if (!check) return;
        check.checked = TABLE[key].contingency;
        check.classList.toggle("is-live", TABLE[key].contingency);
      });
    };

    const enableTry = () => {
      tryMode = true;
      nudgeBtns.forEach((btn) => {
        const key = btn.getAttribute("data-sp-nudge") as SpRowKey | null;
        if (key && NUDGE_CYCLES[key]) {
          btn.disabled = false;
          btn.classList.add("is-pulse");
        }
      });
      if (recalcBtn) {
        recalcBtn.hidden = false;
        recalcBtn.disabled = true;
      }
      if (replayBtn) replayBtn.hidden = false;
      if (hint) hint.hidden = false;
    };

    const paintFinal = () => {
      if (addrEl) addrEl.textContent = ADDRESS;
      if (factsEl) factsEl.textContent = FACTS;
      desc?.classList.add("is-live");
      if (descBody) descBody.style.opacity = "1";
      paintScores();
      fillReno();
      FEEDS.forEach((f) => setFeedLive(f.id));
      paintContingencies();
      setValuation(0, false);
      if (feedPill) feedPill.textContent = "Feeds live";
      if (flags) flags.hidden = false;
      flags?.querySelectorAll(".sp__flag").forEach((el) => el.classList.add("is-live"));
      mapEl?.classList.add("is-live", "is-heat");
      startOverlayCycle();
      if (acceptBtn) {
        acceptBtn.disabled = false;
        acceptBtn.classList.add("is-done");
        acceptBtn.textContent = "Accepted";
      }
      enableTry();
    };

    const finish = () => {
      running = false;
      setStep("Decision recorded · try the Final Data cells");
      enableTry();
      frame.dispatchEvent(new CustomEvent("demo-request-complete", { bubbles: true }));
    };

    const snapFinal = () => {
      resetBoard();
      paintFinal();
      finish();
    };

    const run = async () => {
      const my = ++token;
      running = true;
      tryMode = false;
      resetBoard();

      // 1. File opens
      setStep("File opening…");
      if (addrEl) addrEl.textContent = "Loading #4418…";
      await sleep(1100);
      if (my !== token) return;

      if (addrEl) addrEl.textContent = ADDRESS;
      if (factsEl) factsEl.textContent = FACTS;
      paintScores();
      fillReno();
      desc?.classList.add("is-live");
      if (descBody) descBody.style.opacity = "1";
      mapEl?.classList.add("is-live");
      setStep("File open · Home Fact Verification");
      await sleep(1100);
      if (my !== token) return;

      // 2. Feeds resolve into table cells (working → live)
      setStep("Resolving vendor feeds");
      for (const feed of FEEDS) {
        if (my !== token) return;
        if (feedPill) feedPill.textContent = `Pulling ${feed.label}…`;
        setStep(`Pulling ${feed.label}…`);
        setFeedWorking(feed.id);
        await sleep(feed.progressMs);
        if (my !== token) return;
        setFeedLive(feed.id);
        if (feedPill) feedPill.textContent = `${feed.label} live`;
        await sleep(feed.resolveMs);
        if (my !== token) return;
      }
      if (feedPill) feedPill.textContent = "Feeds live";
      setStep("Vendor feeds resolved");
      await sleep(1050);
      if (my !== token) return;

      // 3. Valuation calculates + neighborhood heat starts
      setStep("Calculating Stoa valuation…");
      setValuation(0, true);
      mapEl?.classList.add("is-heat");
      startOverlayCycle();
      await sleep(2000);
      if (my !== token) return;
      setValuation(0, false);
      setStep("Stoa valuation locked at $392,547");
      await sleep(1400);
      if (my !== token) return;

      // 4. Flags / contingencies
      setStep("Raising contingencies…");
      if (flags) flags.hidden = false;
      const flagIds = ["beds", "lot", "avm"] as const;
      for (const id of flagIds) {
        if (my !== token) return;
        const el = root.querySelector(`[data-sp-flag='${id}']`);
        el?.classList.add("is-working");
        await sleep(380);
        if (my !== token) return;
        el?.classList.remove("is-working");
        el?.classList.add("is-live");
        if (id === "beds" || id === "lot") {
          const check = root.querySelector<HTMLInputElement>(`[data-sp-check='${id}']`);
          if (check) {
            check.checked = true;
            check.classList.add("is-live");
          }
        }
        await sleep(320);
        if (my !== token) return;
      }
      setStep("Contingencies ready for review");
      await sleep(1100);
      if (my !== token) return;

      // 5. Accept
      setStep("Analyst review");
      if (acceptBtn) acceptBtn.disabled = false;
      await sleep(1700);
      if (my !== token) return;

      if (acceptBtn) {
        acceptBtn.classList.add("is-done");
        acceptBtn.textContent = "Accepted";
      }
      setStep("Decision recorded");
      await sleep(1100);
      if (my !== token) return;

      finish();
    };

    frame.addEventListener("demo-play", (e) => {
      const detail = (e as CustomEvent).detail as { reduced?: boolean } | undefined;
      if (detail?.reduced) {
        snapFinal();
        return;
      }
      void run();
    });

    frame.addEventListener("demo-replay", () => void run());

    nudgeBtns.forEach((btn) => {
      btn.addEventListener("click", () => {
        if (!tryMode || running || recalcing) return;
        const key = btn.getAttribute("data-sp-nudge") as SpRowKey | null;
        if (!key) return;
        const cycle = NUDGE_CYCLES[key];
        if (!cycle) return;
        const next = ((nudgeIdx[key] ?? 0) + 1) % cycle.length;
        nudgeIdx[key] = next;
        btn.textContent = cycle[next] ?? cycle[0]!;
        dirty = true;
        btn.classList.add("is-nudge");
        btn.classList.remove("is-pulse");
        setTimeout(() => btn.classList.remove("is-nudge"), 280);
        nudgeBtns.forEach((b) => b.classList.remove("is-pulse"));
        if (recalcBtn) recalcBtn.disabled = false;
        setStep("Final Data nudged · Recalc when ready");
      });
    });

    recalcBtn?.addEventListener("click", async () => {
      if (!tryMode || running || !dirty || recalcing) return;
      recalcing = true;
      dirty = false;
      if (recalcBtn) recalcBtn.disabled = true;
      setStep("Recalculating valuation…");
      setValuation(valuationIdx, true);
      recalcBtn?.classList.add("is-nudge");
      await sleep(1700);
      const nextIdx = (valuationIdx + 1) % VALUATIONS.length;
      setValuation(nextIdx, false);
      setStep(`Valuation updated to ${VALUATIONS[nextIdx]!.price}`);
      setTimeout(() => recalcBtn?.classList.remove("is-nudge"), 280);
      recalcing = false;
    });

    replayBtn?.addEventListener("click", () => {
      if (running || recalcing) return;
      frame.dispatchEvent(new CustomEvent("demo-request-replay", { bubbles: true }));
    });

    acceptBtn?.addEventListener("click", () => {
      if (!tryMode || running || acceptBtn.classList.contains("is-done")) return;
      acceptBtn.classList.add("is-done");
      acceptBtn.textContent = "Accepted";
      setStep("Decision recorded");
    });
  }

  document.querySelectorAll("[data-sp]").forEach((el) => wireStoa(el as HTMLElement));
  document.addEventListener("astro:page-load", () => {
    document.querySelectorAll("[data-sp]").forEach((el) => wireStoa(el as HTMLElement));
  });
</script>

<style>
  .sp {
    --sp-purple: #6d28d9;
    --sp-purple-soft: #8b5cf6;
    --sp-purple-wash: #f3e8ff;
    --sp-ink: #1e1b2e;
    --sp-muted: #6b7280;
    --sp-line: #e5e7eb;
    --sp-panel: #ffffff;
    --sp-bg: #f7f5fb;
    --sp-rail: #1e293b;
    --sp-deal: #16a34a;
    --sp-partner: #ea580c;
    --sp-blue: #2563eb;
    --sp-flag: #ffedd5;

    height: 100%;
    display: grid;
    grid-template-columns: 2.25rem 1fr;
    background: var(--sp-bg);
    color: var(--sp-ink);
    font-family: "Segoe UI", "Helvetica Neue", sans-serif;
    font-size: 0.68rem;
    line-height: 1.35;
    overflow: hidden;
  }

  .sp__rail {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.55rem;
    padding: 0.55rem 0;
    background: var(--sp-rail);
  }

  .sp__rail-mark {
    width: 1.35rem;
    height: 1.35rem;
    display: grid;
    place-items: center;
    border-radius: 0.35rem;
    background: var(--sp-purple);
    color: #fff;
    font-size: 0.62rem;
    font-weight: 700;
    margin-bottom: 0.35rem;
  }

  .sp__rail-dot {
    width: 0.7rem;
    height: 0.7rem;
    border-radius: 0.2rem;
    background: color-mix(in srgb, #94a3b8 55%, transparent);
  }

  .sp__rail-dot.is-active {
    background: var(--sp-purple-soft);
  }

  .sp__app {
    display: grid;
    grid-template-rows: auto auto 1fr;
    min-width: 0;
    min-height: 0;
    background: var(--sp-panel);
  }

  .sp__top {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.45rem 0.75rem;
    border-bottom: 1px solid var(--sp-line);
    background: #fff;
  }

  .sp__brand {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    flex-shrink: 0;
  }

  .sp__logo {
    width: 0.85rem;
    height: 0.85rem;
    background: var(--sp-purple);
    transform: rotate(45deg);
    border-radius: 0.12rem;
  }

  .sp__brand-text {
    display: flex;
    flex-direction: column;
    line-height: 1.1;
  }

  .sp__product {
    font-weight: 700;
    font-size: 0.72rem;
    color: var(--sp-purple);
  }

  .sp__module {
    font-size: 0.55rem;
    color: var(--sp-muted);
    text-transform: uppercase;
    letter-spacing: 0.04em;
  }

  .sp__tabs {
    display: flex;
    flex-wrap: nowrap;
    gap: 0.65rem;
    min-width: 0;
    overflow: hidden;
    flex: 1;
  }

  .sp__tab {
    white-space: nowrap;
    color: var(--sp-muted);
    padding: 0.35rem 0.1rem 0.4rem;
    border-bottom: 2px solid transparent;
    font-size: 0.64rem;
  }

  .sp__tab.is-active {
    color: var(--sp-purple);
    font-weight: 600;
    border-bottom-color: var(--sp-purple);
  }

  .sp__user {
    display: flex;
    align-items: center;
    gap: 0.4rem;
    margin-inline-start: auto;
  }

  .sp__user-icon {
    width: 0.7rem;
    height: 0.7rem;
    border-radius: 50%;
    border: 1.5px solid #c4b5fd;
  }

  .sp__avatar {
    width: 1.35rem;
    height: 1.35rem;
    border-radius: 50%;
    display: grid;
    place-items: center;
    background: var(--sp-purple-wash);
    color: var(--sp-purple);
    font-size: 0.55rem;
    font-weight: 700;
  }

  .sp__status {
    margin: 0;
    padding: 0.28rem 0.75rem;
    border-bottom: 1px solid var(--sp-line);
    background: #f8f7fc;
    color: var(--sp-ink);
    font-size: 0.58rem;
    font-weight: 600;
    letter-spacing: 0.02em;
    text-transform: uppercase;
  }

  .sp__workspace {
    display: grid;
    grid-template-columns: minmax(0, 1.4fr) minmax(11rem, 0.95fr);
    min-height: 0;
    overflow: hidden;
  }

  .sp__details {
    min-width: 0;
    min-height: 0;
    overflow: auto;
    padding: 0.55rem 0.65rem 0.7rem;
    display: grid;
    gap: 0.45rem;
    align-content: start;
    border-right: 1px solid var(--sp-line);
  }

  .sp__meta {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.4rem 0.55rem;
  }

  .sp__back {
    border: 0;
    background: none;
    padding: 0;
    color: var(--sp-muted);
    font: inherit;
    cursor: default;
  }

  .sp__scores {
    display: flex;
    flex-wrap: wrap;
    gap: 0.3rem;
  }

  .sp__score {
    padding: 0.15rem 0.45rem;
    border-radius: 999px;
    font-size: 0.55rem;
    font-weight: 600;
    color: #fff;
    background: #9ca3af;
    opacity: 0.45;
    transition: opacity 200ms ease;
  }

  .sp__score.is-live {
    opacity: 1;
  }

  .sp__score--deal.is-live {
    background: var(--sp-deal);
  }

  .sp__score--partner.is-live {
    background: var(--sp-partner);
  }

  .sp__score--entity.is-live {
    background: var(--sp-deal);
  }

  .sp__addr {
    margin: 0;
    font-size: 0.82rem;
    font-weight: 700;
    letter-spacing: -0.01em;
    color: var(--sp-ink);
  }

  .sp__facts {
    margin: 0;
    color: var(--sp-muted);
    font-size: 0.58rem;
  }

  .sp__summary {
    display: grid;
    grid-template-columns: 1.1fr 1fr;
    gap: 0.55rem;
    align-items: start;
  }

  .sp__desc-title,
  .sp__val-label,
  .sp__table-title {
    margin: 0 0 0.25rem;
    font-size: 0.58rem;
    font-weight: 700;
    color: var(--sp-ink);
  }

  .sp__desc {
    opacity: 0.5;
    transition: opacity 240ms ease;
  }

  .sp__desc.is-live {
    opacity: 1;
  }

  .sp__desc-body {
    margin: 0;
    color: var(--sp-muted);
    font-size: 0.58rem;
  }

  .sp__more {
    color: var(--sp-purple);
    font-weight: 600;
  }

  .sp__val {
    padding: 0.55rem 0.65rem;
    border: 1px solid var(--sp-line);
    border-radius: 0.5rem;
    background: #fff;
    box-shadow: 0 1px 2px color-mix(in srgb, #000 4%, transparent);
    transition:
      border-color 220ms ease,
      box-shadow 220ms ease;
  }

  .sp__val.is-working {
    border-color: color-mix(in srgb, var(--sp-purple) 45%, var(--sp-line));
    box-shadow: inset 0 0 0 1px color-mix(in srgb, var(--sp-purple) 18%, transparent);
    animation: sp-working 1.35s ease-in-out infinite;
  }

  .sp__val.is-live {
    border-color: color-mix(in srgb, var(--sp-purple) 40%, var(--sp-line));
  }

  .sp__val-price {
    margin: 0 0 0.35rem;
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
    gap: 0.35rem;
  }

  .sp__val-price strong {
    font-size: 1.05rem;
    color: var(--sp-blue);
    font-weight: 700;
  }

  .sp__val.is-working .sp__val-price strong {
    color: var(--sp-purple-soft);
  }

  .sp__val-psf {
    color: var(--sp-muted);
    font-size: 0.55rem;
  }

  .sp__range {
    opacity: 0.35;
    transition: opacity 240ms ease;
  }

  .sp__range.is-live {
    opacity: 1;
  }

  .sp__range-track {
    position: relative;
    height: 0.35rem;
    border-radius: 999px;
    background: linear-gradient(90deg, #93c5fd, var(--sp-purple-soft), #c4b5fd);
  }

  .sp__range-fill {
    display: none;
  }

  .sp__range-marker {
    position: absolute;
    top: -0.2rem;
    left: 48%;
    width: 0.2rem;
    height: 0.75rem;
    border-radius: 2px;
    background: var(--sp-blue);
  }

  .sp__range-labels {
    display: flex;
    justify-content: space-between;
    gap: 0.35rem;
    margin-top: 0.25rem;
    font-size: 0.48rem;
    color: var(--sp-muted);
  }

  .sp__table-wrap {
    min-width: 0;
  }

  .sp__table-head {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 0.5rem;
    margin-bottom: 0.25rem;
  }

  .sp__feed-pill {
    font-size: 0.5rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.03em;
    color: var(--sp-purple);
    background: var(--sp-purple-wash);
    padding: 0.12rem 0.4rem;
    border-radius: 999px;
  }

  .sp__table-scroll {
    overflow: auto;
    max-height: 11.5rem;
    border: 1px solid var(--sp-line);
    border-radius: 0.4rem;
  }

  .sp__table {
    width: 100%;
    border-collapse: collapse;
    font-size: 0.6rem;
    white-space: nowrap;
  }

  .sp__table th,
  .sp__table td {
    padding: 0.32rem 0.4rem;
    border-bottom: 1px solid var(--sp-line);
    text-align: left;
    color: var(--sp-ink);
  }

  .sp__table th {
    position: sticky;
    top: 0;
    background: #f9fafb;
    color: var(--sp-muted);
    font-weight: 600;
    z-index: 1;
  }

  .sp__table th.is-working {
    color: var(--sp-purple);
    animation: sp-working 1.35s ease-in-out infinite;
  }

  .sp__table th.is-live {
    color: var(--sp-purple);
  }

  .sp__table td.is-working {
    color: var(--sp-purple-soft);
    animation: sp-working 1.35s ease-in-out infinite;
  }

  .sp__table td.is-live {
    color: var(--sp-ink);
  }

  .sp__row--flag td:first-child {
    background: var(--sp-flag);
    font-weight: 600;
  }

  .sp__final {
    min-width: 3.6rem;
    padding: 0.15rem 0.35rem;
    border: 1px solid var(--sp-line);
    border-radius: 0.3rem;
    background: #fff;
    color: var(--sp-ink);
    font: inherit;
    text-align: left;
    cursor: default;
  }

  .sp__final:not(:disabled) {
    cursor: pointer;
    border-color: color-mix(in srgb, var(--sp-purple) 45%, var(--sp-line));
    color: var(--sp-purple);
  }

  .sp__final.is-working {
    color: var(--sp-purple-soft);
    border-color: var(--sp-purple-soft);
    animation: sp-working 1.35s ease-in-out infinite;
  }

  .sp__final.is-live {
    color: var(--sp-ink);
  }

  .sp__final.is-pulse {
    animation: sp-pulse 1.55s ease-out infinite;
  }

  .sp__final.is-nudge {
    background: var(--sp-purple-wash);
    color: var(--sp-purple);
  }

  .sp__check {
    width: 0.85rem;
    height: 0.85rem;
    accent-color: var(--sp-purple);
    pointer-events: none;
  }

  .sp__check.is-live {
    outline: 1px solid color-mix(in srgb, var(--sp-purple) 35%, transparent);
  }

  .sp__flags {
    margin: 0;
    padding: 0;
    list-style: none;
    display: flex;
    flex-wrap: wrap;
    gap: 0.3rem;
  }

  .sp__flag {
    padding: 0.18rem 0.45rem;
    border-radius: 0.3rem;
    border: 1px solid var(--sp-line);
    background: #fff;
    color: var(--sp-muted);
    font-size: 0.52rem;
    opacity: 0.35;
    transition:
      opacity 180ms ease,
      border-color 180ms ease,
      color 180ms ease;
  }

  .sp__flag.is-working {
    opacity: 1;
    color: var(--sp-purple);
    border-color: color-mix(in srgb, var(--sp-purple) 45%, var(--sp-line));
    animation: sp-working 1.35s ease-in-out infinite;
  }

  .sp__flag.is-live {
    opacity: 1;
    color: var(--sp-partner);
    border-color: color-mix(in srgb, var(--sp-partner) 40%, var(--sp-line));
    background: #fff7ed;
  }

  .sp__actions {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.4rem;
  }

  .sp__accept,
  .sp__recalc,
  .sp__replay {
    padding: 0.35rem 0.75rem;
    border-radius: 0.35rem;
    border: 1px solid var(--sp-line);
    background: #fff;
    color: var(--sp-ink);
    font: inherit;
    font-weight: 600;
    font-size: 0.58rem;
    cursor: pointer;
  }

  .sp__replay {
    display: none !important;
  }

  .sp__accept:disabled {
    opacity: 0.45;
    cursor: default;
  }

  .sp__accept.is-done {
    background: var(--sp-purple);
    border-color: var(--sp-purple);
    color: #fff;
  }

  .sp__recalc {
    border-color: color-mix(in srgb, var(--sp-purple) 45%, var(--sp-line));
    color: var(--sp-purple);
  }

  .sp__recalc:disabled {
    opacity: 0.45;
    cursor: default;
  }

  .sp__recalc.is-nudge {
    background: var(--sp-purple-wash);
  }

  .sp__hint {
    margin: 0;
    color: var(--sp-muted);
    font-size: 0.52rem;
  }

  .sp__map {
    min-width: 0;
    min-height: 0;
    display: grid;
    grid-template-rows: auto 1fr auto;
    background: #e8eef5;
    opacity: 0.7;
    transition: opacity 280ms ease;
  }

  .sp__map.is-live {
    opacity: 1;
  }

  .sp__map-chrome {
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 0.4rem;
    padding: 0.4rem;
    position: relative;
    z-index: 2;
  }

  .sp__map-modes {
    display: flex;
    gap: 0.15rem;
    background: rgba(255, 255, 255, 0.92);
    border-radius: 0.35rem;
    padding: 0.12rem;
    box-shadow: 0 1px 3px rgba(15, 23, 42, 0.12);
  }

  .sp__map-mode {
    padding: 0.18rem 0.4rem;
    border-radius: 0.25rem;
    color: var(--sp-ink);
    font-size: 0.5rem;
    font-weight: 600;
  }

  .sp__map-mode.is-active {
    background: var(--sp-purple);
    color: #fff;
  }

  .sp__map-layer-pill {
    margin-left: auto;
    margin-right: 0.35rem;
    padding: 0.18rem 0.45rem;
    border-radius: 999px;
    background: rgba(15, 23, 42, 0.82);
    color: #fff;
    font-size: 0.5rem;
    font-weight: 700;
    letter-spacing: 0.03em;
    text-transform: uppercase;
  }

  .sp__map-zoom {
    display: flex;
    flex-direction: column;
    gap: 0.15rem;
  }

  .sp__map-zoom span {
    width: 1.1rem;
    height: 1.1rem;
    display: grid;
    place-items: center;
    border-radius: 0.25rem;
    background: rgba(255, 255, 255, 0.95);
    color: var(--sp-ink);
    font-size: 0.7rem;
    font-weight: 700;
    box-shadow: 0 1px 3px rgba(15, 23, 42, 0.14);
  }

  .sp__map-canvas {
    min-height: 0;
    overflow: hidden;
    position: relative;
  }

  .sp__gmap {
    position: absolute;
    inset: 0;
    overflow: hidden;
    background:
      linear-gradient(180deg, #dfe8f2 0%, #e8eef5 40%, #dde7f0 100%);
  }

  .sp__gmap-pan {
    position: absolute;
    inset: -8%;
    animation: sp-map-pan 18s ease-in-out infinite alternate;
  }

  .sp__map.is-heat .sp__gmap-pan {
    animation-duration: 14s;
  }

  .sp__gmap-roads {
    position: absolute;
    inset: 0;
    background:
      linear-gradient(90deg, transparent 18%, #fff 18.5%, #fff 20%, transparent 20.5%),
      linear-gradient(90deg, transparent 52%, #f8fafc 52.4%, #f8fafc 54%, transparent 54.4%),
      linear-gradient(90deg, transparent 78%, #fff 78.3%, #fff 79.5%, transparent 79.8%),
      linear-gradient(0deg, transparent 28%, #fff 28.4%, #fff 30%, transparent 30.4%),
      linear-gradient(0deg, transparent 62%, #f8fafc 62.3%, #f8fafc 64%, transparent 64.3%),
      linear-gradient(35deg, transparent 44%, rgba(255, 255, 255, 0.85) 44.4%, rgba(255, 255, 255, 0.85) 45.6%, transparent 46%),
      repeating-linear-gradient(
        0deg,
        transparent,
        transparent 28px,
        rgba(148, 163, 184, 0.12) 28px,
        rgba(148, 163, 184, 0.12) 29px
      ),
      repeating-linear-gradient(
        90deg,
        transparent,
        transparent 34px,
        rgba(148, 163, 184, 0.1) 34px,
        rgba(148, 163, 184, 0.1) 35px
      );
  }

  .sp__gmap-park {
    position: absolute;
    left: 8%;
    top: 56%;
    width: 28%;
    height: 22%;
    border-radius: 40% 45% 38% 50%;
    background: #c5dfb8;
    opacity: 0.85;
  }

  .sp__gmap-water {
    position: absolute;
    right: -5%;
    top: 8%;
    width: 34%;
    height: 36%;
    border-radius: 45% 40% 50% 42%;
    background: #a8c8e8;
    opacity: 0.9;
  }

  .sp__gmap-label {
    position: absolute;
    font-size: 0.48rem;
    font-weight: 600;
    color: #475569;
    letter-spacing: 0.02em;
    text-shadow: 0 1px 0 rgba(255, 255, 255, 0.8);
    white-space: nowrap;
  }

  .sp__gmap-label--place {
    color: #334155;
    font-weight: 700;
    font-size: 0.55rem;
  }

  .sp__pin {
    position: absolute;
    left: 56%;
    top: 44%;
    width: 0.7rem;
    height: 0.7rem;
    margin: -0.35rem 0 0 -0.35rem;
    border-radius: 50% 50% 50% 0;
    transform: rotate(-45deg);
    background: var(--sp-purple);
    box-shadow: 0 0 0 2px #fff, 0 2px 6px rgba(15, 23, 42, 0.35);
    z-index: 3;
  }

  .sp__octs {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    opacity: 0;
    transition: opacity 450ms ease;
    pointer-events: none;
    z-index: 2;
  }

  .sp__map.is-heat .sp__octs {
    opacity: 1;
  }

  .sp__octs g {
    transition: opacity 400ms ease;
  }

  .sp__octs g[hidden] {
    display: none;
  }

  .sp__map-foot {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    justify-content: space-between;
    gap: 0.35rem;
    padding: 0.35rem 0.45rem;
    background: rgba(255, 255, 255, 0.96);
    border-top: 1px solid color-mix(in srgb, var(--sp-line) 80%, transparent);
    z-index: 2;
  }

  .sp__layers {
    display: flex;
    flex-wrap: wrap;
    gap: 0.25rem;
  }

  .sp__layer {
    appearance: none;
    border: 1px solid transparent;
    background: transparent;
    font-size: 0.5rem;
    color: var(--sp-muted);
    font-weight: 600;
    padding: 0.12rem 0.35rem;
    border-radius: 999px;
    cursor: default;
  }

  .sp__layer.is-active {
    color: #fff;
    background: var(--sp-purple);
    border-color: var(--sp-purple);
  }

  .sp__view-images {
    font-size: 0.52rem;
    font-weight: 700;
    color: var(--sp-purple);
  }

  @keyframes sp-map-pan {
    0% {
      transform: translate3d(0, 0, 0) scale(1.05);
    }
    100% {
      transform: translate3d(-3.5%, -2.5%, 0) scale(1.12);
    }
  }

  @keyframes sp-working {
    0%,
    100% {
      opacity: 1;
    }
    50% {
      opacity: 0.72;
    }
  }

  @keyframes sp-pulse {
    0%,
    100% {
      box-shadow: 0 0 0 0 color-mix(in srgb, var(--sp-purple) 0%, transparent);
    }
    50% {
      box-shadow: 0 0 0 3px color-mix(in srgb, var(--sp-purple) 28%, transparent);
    }
  }

  @media (max-width: 40rem) {
    .sp {
      grid-template-columns: 1fr;
      font-size: 0.62rem;
    }

    .sp__rail {
      display: none;
    }

    .sp__top {
      padding: 0.3rem 0.5rem;
      gap: 0.45rem;
    }

    .sp__status {
      padding: 0.22rem 0.5rem;
      font-size: 0.52rem;
    }

    .sp__workspace {
      grid-template-columns: 1fr;
      grid-template-rows: minmax(0, 1fr) 7.5rem;
    }

    .sp__details {
      border-right: 0;
      border-bottom: 1px solid var(--sp-line);
      order: 1;
      overflow: hidden;
      padding: 0.4rem 0.5rem 0.45rem;
      gap: 0.3rem;
    }

    .sp__map {
      display: grid;
      order: 2;
      min-height: 7.5rem;
      border-top: 0;
    }

    .sp__map-modes .sp__map-mode:nth-child(n + 3),
    .sp__map-zoom {
      display: none;
    }

    .sp__map-foot {
      padding: 0.22rem 0.35rem;
    }

    .sp__view-images {
      display: none;
    }

    .sp__summary {
      grid-template-columns: 1fr;
      gap: 0.35rem;
    }

    .sp__desc {
      display: none;
    }

    .sp__val {
      padding: 0.45rem 0.55rem;
      order: -1;
    }

    .sp__val-label {
      font-size: 0.55rem;
      text-transform: uppercase;
      letter-spacing: 0.04em;
      color: var(--sp-purple);
    }

    .sp__val-price strong {
      font-size: 1.2rem;
    }

    .sp__range-labels {
      font-size: 0.44rem;
    }

    .sp__addr {
      font-size: 0.72rem;
      line-height: 1.25;
    }

    .sp__facts {
      font-size: 0.52rem;
    }

    .sp__scores {
      gap: 0.2rem;
    }

    .sp__score {
      font-size: 0.48rem;
      padding: 0.1rem 0.35rem;
    }

    .sp__table-scroll {
      overflow: hidden;
      max-height: none;
    }

    .sp__table {
      font-size: 0.5rem;
    }

    .sp__table th,
    .sp__table td {
      padding: 0.2rem 0.28rem;
    }

    .sp__table th:nth-child(2),
    .sp__table td:nth-child(2),
    .sp__table th:nth-child(5),
    .sp__table td:nth-child(5),
    .sp__table th:nth-child(7),
    .sp__table td:nth-child(7) {
      display: none;
    }

    .sp__final {
      min-width: 2.4rem;
      padding: 0.1rem 0.25rem;
    }

    .sp__flags {
      gap: 0.2rem;
    }

    .sp__flag {
      font-size: 0.48rem;
      padding: 0.12rem 0.35rem;
    }

    .sp__actions {
      gap: 0.3rem;
    }

    .sp__accept,
    .sp__recalc,
    .sp__replay {
      padding: 0.28rem 0.55rem;
      font-size: 0.52rem;
    }

    .sp__tabs .sp__tab:nth-child(n + 4) {
      display: none;
    }

    .sp__tab {
      font-size: 0.55rem;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .sp__val.is-working,
    .sp__table th.is-working,
    .sp__table td.is-working,
    .sp__final.is-working,
    .sp__final.is-pulse,
    .sp__flag.is-working,
    .sp__gmap-pan {
      animation: none;
      transition: none;
    }
  }
</style>
